import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import ClientBody from "./ClientBody";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Donaite - AI-Powered Global Charity Donation Platform",
  description: "Connect with reputable charities worldwide and make a difference through AI-driven donation suggestions and impact visualization.",
  keywords: ["charity", "donation", "AI", "global giving", "philanthropy", "impact", "nonprofit"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`} suppressHydrationWarning>
      <ClientBody>{children}</ClientBody>
    </html>
  );
}
